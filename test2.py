def powu(a: float, b: int) -> float:
    res = 1.0
    x = 1

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    # Bit 0 (value 1)
    if x <= -b:
        if int(b / x) % 2 == 1:
            if b < 0:
                res = res / a
            elif b > 0:
                res = res * a
        a = a * a
        x = x * 2

    return res